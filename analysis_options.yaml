include: package:lints/recommended.yaml
analyzer:
  exclude:
    # Standalone Flutter projects with their own pubspec.yaml. Analyzing them
    # from this pure-Dart package's context cannot resolve the Flutter SDK,
    # so every Flutter symbol is reported as undefined.
    - test/fixtures/**
    - example/sample_app/**
