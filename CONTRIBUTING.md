# Contributing

Run `dart format .`, `dart analyze`, and `dart test` before submitting a pull request.
Keep generation conservative: never modify application files and never overwrite handwritten tests.
